'use strict';

describe('Controller: OrderShippingWeekCtrl', function () {

  // load the controller's module
  beforeEach(module('employeeApp'));

  var OrderShippingWeekCtrl,
    scope;

  // Initialize the controller and a mock scope
  beforeEach(inject(function ($controller) {
    scope = {};
    OrderShippingWeekCtrl = $controller('OrderShippingWeekCtrl', {
      $scope: scope
    });
  }));

  
});
