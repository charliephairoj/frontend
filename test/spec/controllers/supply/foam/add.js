'use strict';

describe('Controller: SupplyFoamAddCtrl', function () {

  // load the controller's module
  beforeEach(module('employeeApp'));

  var SupplyFoamAddCtrl,
    scope;

  // Initialize the controller and a mock scope
  beforeEach(inject(function ($controller) {
    scope = {};
    SupplyFoamAddCtrl = $controller('SupplyFoamAddCtrl', {
      $scope: scope
    });
  }));

  
});
