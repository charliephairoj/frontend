'use strict';

describe('Controller: SupplyFoamDetailsCtrl', function () {

  // load the controller's module
  beforeEach(module('employeeApp'));

  var SupplyFoamDetailsCtrl,
    scope;

  // Initialize the controller and a mock scope
  beforeEach(inject(function ($controller) {
    scope = {};
    SupplyFoamDetailsCtrl = $controller('SupplyFoamDetailsCtrl', {
      $scope: scope
    });
  }));

  
});
