'use strict';

describe('Controller: SupplyFoamViewCtrl', function () {

  // load the controller's module
  beforeEach(module('employeeApp'));

  var SupplyFoamViewCtrl,
    scope;

  // Initialize the controller and a mock scope
  beforeEach(inject(function ($controller) {
    scope = {};
    SupplyFoamViewCtrl = $controller('SupplyFoamViewCtrl', {
      $scope: scope
    });
  }));

  
});
