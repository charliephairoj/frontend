'use strict';

describe('Service: models/deal', function () {

  // load the service's module
  beforeEach(module('frontendApp'));

  // instantiate service
  var models/deal;
  beforeEach(inject(function (_models/deal_) {
    models/deal = _models/deal_;
  }));

  it('should do something', function () {
    expect(!!models/deal).toBe(true);
  });

});
