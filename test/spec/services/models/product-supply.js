'use strict';

describe('Service: models/productSupply', function () {

  // load the service's module
  beforeEach(module('frontendApp'));

  // instantiate service
  var models/productSupply;
  beforeEach(inject(function (_models/productSupply_) {
    models/productSupply = _models/productSupply_;
  }));

  it('should do something', function () {
    expect(!!models/productSupply).toBe(true);
  });

});
