'use strict';

describe('Service: models/configuration', function () {

  // load the service's module
  beforeEach(module('employeeApp.services'));

  // instantiate service
  beforeEach(inject(function() {
  }));

  
});
