'use strict';

xdescribe('Service: models/equipment', function () {

  // load the service's module
  beforeEach(module('frontApp'));

  // instantiate service
  var equipment;
  beforeEach(inject(function (_equipment_) {
  }));

  it('should do something', function () {
  });

});
