'use strict';

describe('Service: models/phase', function () {

  // load the service's module
  beforeEach(module('frontendApp'));

  // instantiate service
  var models/phase;
  beforeEach(inject(function (_models/phase_) {
    models/phase = _models/phase_;
  }));

  it('should do something', function () {
    expect(!!models/phase).toBe(true);
  });

});
