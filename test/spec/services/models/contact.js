'use strict';

xdescribe('Service: models/contact', function () {

  // load the service's module
  beforeEach(module('employeeApp'));

  // instantiate service
  beforeEach(inject(function() {
  }));

  it('should do something', function () {
  });

});
