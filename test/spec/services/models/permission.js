'use strict';

describe('Service: models/permission', function () {

  // load the service's module
  beforeEach(module('employeeApp'));

  // instantiate service
  beforeEach(inject(function(_Permission_) {
  }));

  it('should do something', function () {
  });

});
