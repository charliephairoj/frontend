'use strict';

describe('Service: models/shift', function () {

  // load the service's module
  beforeEach(module('frontendApp'));

  // instantiate service
  var models/shift;
  beforeEach(inject(function (_models/shift_) {
    models/shift = _models/shift_;
  }));

  it('should do something', function () {
    expect(!!models/shift).toBe(true);
  });

});
