'use strict';

describe('Service: models/payroll', function () {

  // load the service's module
  beforeEach(module('frontendApp'));

  // instantiate service
  var models/payroll;
  beforeEach(inject(function (_models/payroll_) {
    models/payroll = _models/payroll_;
  }));

  it('should do something', function () {
    expect(!!models/payroll).toBe(true);
  });

});
