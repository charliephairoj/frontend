'use strict';

describe('Service: interceptors/dateParser', function () {

  // load the service's module
  beforeEach(module('employeeApp.services'));

  // instantiate service
  beforeEach(inject(function(_dateParser_) {
  }));

 
});
