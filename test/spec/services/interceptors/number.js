'use strict';

describe('Service: interceptors/number', function () {

  // load the service's module
  beforeEach(module('frontendApp'));

  // instantiate service
  var interceptors/number;
  beforeEach(inject(function (_interceptors/number_) {
    interceptors/number = _interceptors/number_;
  }));

  it('should do something', function () {
    expect(!!interceptors/number).toBe(true);
  });

});
