'use strict';

describe('Service: map/marker', function () {

  // load the service's module
  beforeEach(module('employeeApp'));

  // instantiate service
  var marker;
  beforeEach(inject(function(_marker_) {
    marker;
  }));

  
});
